{
  "title": "PiFire",
  "config": {
    "step": {
      "user": {
        "title": "Connect to PiFire",
        "description": "Enter your PiFire host or IP (no http, no /api).",
        "data": { "host": "Host/IP" }
      }
    },
    "abort": { "already_configured": "This PiFire is already set up." }
  }
}
